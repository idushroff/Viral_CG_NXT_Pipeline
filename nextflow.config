// nextflow.config

process {
    executor = 'local'  // Change to 'slurm', 'pbs', etc. if on an HPC cluster
    withLabel: docker {
        container = ''
        containerEngine = 'docker'  
    }

    cpus = 8
    memory = '16 GB'

}

docker {
    enabled = true
}

